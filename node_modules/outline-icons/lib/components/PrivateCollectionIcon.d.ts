import React from "react";
import { Props } from "./Icon";
export default function PrivateCollectionIcon({ expanded, ...rest }: Props & {
    expanded?: boolean;
}): React.ReactElement<React.ComponentProps<any>, any>;
